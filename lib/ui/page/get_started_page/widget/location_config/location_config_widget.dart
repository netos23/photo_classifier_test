import 'package:flutter/material.dart';

class LocationConfig extends StatelessWidget {
  const LocationConfig({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
